import React from "react";

// This is part 1 in context
const UserContext = React.createContext();

export default UserContext;

/*
In Context we always need a provider So we made a UserContextProvider.jsx
we will make provider like this (sample)
<UserContext>
-- Other Link --
</UserContext/>

*/
